import ./slot_place.chatito
import ./slot_place_attribute.chatito
import ./slot_place_type.chatito

%[ask_attribute]('training': '500')
    what is the @[attribute] of @[place]
    what is the @[attribute] of the @[place_type] @[place]
